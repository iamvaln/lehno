La pastille — le `h` seul, la même lettre partout.

```jsx
<BrandMark size={32} />
<BrandMark variant="favicon" size={28} />
```

**Piège de nommage** : la variante `encre` (dite « sombre » dans la charte) est faite pour un fond **clair** — sur une page sombre elle ne mesure que 1,14:1. Sur tout fond foncé, garder `violet`. Jamais sous 28 px, jamais étirée, jamais recolorée hors palette.
